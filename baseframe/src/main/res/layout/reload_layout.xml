<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/app_background"
        android:gravity="center"
        android:orientation="vertical">

    <ImageView
            android:id="@+id/reloadImg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_no_net" />

    <TextView
            android:id="@+id/reloadText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/item_margin"
            android:gravity="center"
            android:textColor="@color/abstract_text"
            android:textSize="@dimen/headLine"
            tools:text="网络不给力，点击页面再试试吧～" />

    <Button
            android:id="@+id/reloadRefresh"
            android:layout_width="190dp"
            android:layout_height="45dp"
            android:layout_marginTop="@dimen/large_item_margin"
            android:background="@drawable/common_button_selector"
            android:gravity="center"
            android:text="刷新"
            android:textColor="@color/primary_light"
            android:textSize="@dimen/titleSize" />
</LinearLayout>